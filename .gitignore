# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/

# Environments and credentials
.env
*.key
*.pem

# Results and heavy data
resultados/
work/
cribado*/
poses/
Complejos_Fusionados/
fused_complexes/
xml_plip/
san/
*.pdbqt
*.dcd
*.zip

# Downloaded models
*.pt
*.ckpt

# Editor and tooling state: a developer's machine, not part of the product
.ipynb_checkpoints/
.claude/
.vscode/
.idea/
# Personal MCP server config (memory, etc.): a developer's tool, not part of the product,
# and it must never reach the public repo.
.mcp.json

# Operating system
.DS_Store
Thumbs.db

# Metadata Windows attaches to downloads
*:Zone.Identifier

# ADCP working files
tmp_*/
ramaprob.data
installer/vendor/

# codebase-memory-mcp index (local, rebuildable)
.codebase-memory/

# CAVER leaves this where it was started from
whole_clustering.csv
